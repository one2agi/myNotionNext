{
  "mcpServers": {
    "notion": {
      "_comment": "Notion 官方 MCP server (https://github.com/makenotion/notion-mcp-server · npm: @notionhq/notion-mcp-server). Token 走 env, 不入 git.",
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "NOTION_TOKEN": "${env:NOTION_TOKEN}"
      }
    }
  }
}
